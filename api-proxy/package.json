{
  "name": "api-proxy",
  "version": "0.0.0",
  "description": "Adds CORS headers to Reddit's API",
  "main": "api-proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "dependencies": {
    "request": "~2.27.0"
  }
}
